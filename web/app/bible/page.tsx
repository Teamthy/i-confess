import { BiblePage } from "@/components/bible";

export const metadata = {
  title: "Bible",
  description:
    "The verses behind the words. Read the Scripture every confession stands on, served rights-gated from the iCONFESS API.",
};

export default function Page() {
  return <BiblePage />;
}
