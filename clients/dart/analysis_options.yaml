include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
  errors:
    # A credential client should not ship with unused imports or dead code.
    unused_import: error
    unused_local_variable: error
